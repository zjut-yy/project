import { Scale } from '../interfaces';
export declare const CSS: {
    container: string;
    containerNoSpacing: string;
    heading: string;
    scale: (scale: Scale) => "scale--s" | "scale--m" | "scale--l";
};
