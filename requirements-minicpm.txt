# MiniCPM-S / visualization backend environment

# Core API server
fastapi==0.123.0
uvicorn[standard]==0.38.0
pydantic==2.12.2
httpx==0.28.1

# Numeric / trajectory processing
numpy==2.2.6
scikit-learn==1.7.2
PyYAML==6.0.3

# Video and image processing
opencv-python==4.11.0.86
Pillow==12.0.0
decord==0.6.0

# InternVideo / Qwen local inference support
# Install torch/torchvision with the CUDA build matching your machine if needed.
torch>=2.3.0
torchvision>=0.18.0
transformers>=4.40.0
accelerate>=0.30.0
safetensors>=0.4.3
einops>=0.7.0
sentencepiece>=0.2.0

# Optional OpenAI-compatible client used by backend/services/garbage_video_pipeline.py
openai==2.7.1
